{
  "mcpServers": {
    "homeassistant": {
      "command": "uvx",
      "args": ["hass-mcp"],
      "env": {
        "HA_URL": "${HA_URL}",
        "HA_TOKEN": "${HA_TOKEN}"
      },
      "disabled": false
    }
  },
  "_comment": "MCP server using hass-mcp package via uvx.",
  "_instructions": "See CLAUDE.md section 'MCP Server Configuration' for setup instructions."
}
